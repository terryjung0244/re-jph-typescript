export interface ButtonPropsType {
  onClickFunc: () => void; //()안에는 params받는곳
  buttonText: string;
  width?: string;
  height?: string;
  fontSize: string;
  borderRadius: string;
  cursor: string;
  color: string;
}
